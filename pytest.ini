[pytest]
norecursedirs = tmp.* build benchmark _site OLD
addopts = -p no:warnings --auto-parallel
testpaths =
    tests/basic
    tests/help
    tests/browser
    tests/scrape

env =
    AIDER_ANALYTICS=false
